// Criando um objeto
let pessoa = {
  nome: 'Carlos',
  idade: 28,
  cidade: 'Curitiba',
  logado: true
};

// Acessando propriedades (duas formas)
console.log(pessoa.nome);      // 'Carlos' (notação de ponto)
console.log(pessoa['idade']);  // 28 (notação de colchetes)