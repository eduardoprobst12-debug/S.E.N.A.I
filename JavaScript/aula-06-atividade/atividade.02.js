//Use um for para exibir uma contagem regressiva de 10 até 1. Dica: comece com let i = 10, condição i >= 1 e decremente com i--.

let count = 10;

while (count >= 1) {
    console.log (count)
    count--
}